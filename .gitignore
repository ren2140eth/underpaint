node_modules/
.next/
out/
dist/
.env
.env*.local
.DS_Store
*.tsbuildinfo

# local caches — regenerate with `npm run ingest`
.cache/
data/strokes/

# internal planning docs — this repo is public (see public-repo-prod-code-only)
docs/superpowers/
# empty today, but /plan writes specs and briefs here; never publish them
specs/

# next build output
/out/

# playwright-mcp scratch
.playwright-mcp/

# generated by next; contents differ between `next dev` and `next build`
next-env.d.ts

# projected from data/index.json by `npm run palettes`, which prebuild runs
public/palettes.json
.vercel
